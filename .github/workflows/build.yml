name: Build APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install patchelf
        run: sudo apt-get update -qq && sudo apt-get install -y -qq patchelf

      - name: Build opencode assets (binary + glibc)
        run: bash build_assets.sh

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Release APK
        run: ./gradlew assembleRelease --no-daemon

      - name: Find and upload APK
        run: |
          echo "Looking for APK..."
          find app/build -name "*.apk" -type f 2>/dev/null || echo "No APK found in app/build"
          ls -la app/build/outputs/apk/ 2>/dev/null || echo "No outputs/apk directory"
          ls -la app/build/outputs/ 2>/dev/null || echo "No outputs directory"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: opencode-android-release
          path: app/build/outputs/apk/release/*.apk
